/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'standalone',
  // Note: appDir is enabled by default in Next.js 14
};

module.exports = nextConfig;